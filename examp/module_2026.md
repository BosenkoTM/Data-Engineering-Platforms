# Комплексный модульный экзамен

## Теоретические вопросы

1.  **Роль данных в архитектуре предприятия.** Опишите цепочку создания ценности через данные (Analytics Value Chain). Как эта концепция связана с тремя ключевыми группами стейкхолдеров (владельцы, сотрудники, клиенты)? Объясните, как меняется роль Инженера Данных в зависимости от типа (Технический vs Продуктовый) при прохождении данных от состояния Raw Data до Insights.

2.  **Эволюция архитектур аналитических решений.** Сравните классическую архитектуру Хранилища Данных (DWH) и современную концепцию Data Lakehouse. Опишите 5 уровней архитектуры современного аналитического решения (от источников до Governance). В чем заключаются ключевые преимущества облачной архитектуры (на примере GCP) перед on-premise решениями?

3.  **Реляционные СУБД и Моделирование данных.** В чем принципиальное различие между OLTP и OLAP системами? Опишите процесс проектирования хранилища: от концептуальной модели до физической реализации. В чем суть подхода Dimensional Modeling (схема «Звезда»), и зачем нужны таблицы фактов и таблицы измерений?

4.  **Парадигмы обработки данных: ETL vs ELT.** Раскройте понятия ETL и ELT. Почему в современной инженерии данных происходит сдвиг в сторону ELT (на примере связки Cloud DWH + dbt)? Опишите роль Staging Area и как она помогает обеспечить идемпотентность и качество данных при трансформации.

5.  **Инструментарий Data Engineering.** Проведите сравнительный анализ инструментов визуального ETL (например, Pentaho DI, Alteryx) и подхода "Code-first" (Airflow, dbt). Какие из подсистем ETL по Кимбаллу являются критически важными для обеспечения надежности пайплайна (упомяните обработку ошибок и управление метаданными)?

6.  **Business Intelligence и Визуализация.** Опишите архитектуру типичного BI-инструмента (Коннекторы -> Подготовка -> Движок -> Визуализация). В чем разница между классическим BI и Self-Service BI? Перечислите основные принципы проектирования эффективных дашбордов, которые позволяют избежать визуального «шума» и искажения информации.

7.  **Оркестрация и автоматизация.** Что такое DAG (Directed Acyclic Graph) в контексте Apache Airflow и какую роль он играет в управлении данными? Объясните концепцию идемпотентности в оркестрации: почему важно, чтобы повторный запуск задачи давал тот же результат, и как это реализуется технически (например, через `WRITE_TRUNCATE` или партиционирование)?

8.  **Качество данных и Data Governance.**  Как внедрение dbt меняет подход к тестированию данных (Generic tests: unique, not_null, relationships)? Объясните роль документации (Data Catalog, dbt docs) и тестов в обеспечении доверия бизнеса к аналитическим витринам.

---

# Практические задания


**Задание 1. Развертывание базы данных и SQL-моделирование.**

*Задача:* Развернуть PostgreSQL в Docker-контейнере. Спроектировать и реализовать физическую модель данных (схема «Звезда») для dataset "Superstore". Загрузить сырые данные.

*Результат:* SQL-скрипты (DDL, INSERT), скриншот успешных запросов к таблицам фактов и измерений.

**Задание 2. Построение DWH с использованием dbt (Data Build Tool).**

*Задача:* Развернуть dbt-проект поверх PostgreSQL. Реализовать многоуровневую архитектуру (Staging -> Intermediate -> Marts). Создать витрину данных для анализа "спящих ценных клиентов" (`mart_valuable_dormant_customers`). Написать тесты качества данных.

*Результат:* Ссылка на репозиторий с кодом моделей, скриншот графа зависимостей (Lineage) и успешного прохождения тестов (`dbt test`).

**Задание 3. Создание BI-дашборда в Yandex DataLens.**

*Задача:* Подключить BI-систему к созданному DWH. Разработать интерактивный дашборд для мониторинга KPI продаж и LTV клиентов. Реализовать минимум 3 типа визуализаций (линейный график, стрим-чарт или таблица, индикаторы).

*Результат:* Скриншоты дашборда, описание ответов на ключевые бизнес-вопросы, полученные из графиков.

**Задание 4. Оркестрация процессов с Apache Airflow.**

*Задача:* Написать DAG на Python для Apache Airflow (развернутого локально в Docker), который автоматизирует запуск dbt-моделей. DAG должен включать шаги: запуск моделей (`dbt run`) и тестирование (`dbt test`).

*Результат:* Файл `dag.py`, скриншот Airflow UI с успешным выполнением графа (все задачи зеленые).

---

## Подробное руководство по выполнению практических заданий

**Используемый стек:** Docker, PostgreSQL, dbt (data build tool), Apache Airflow, Yandex DataLens.

**Данные:** Dataset "Superstore" (Продажи).



## 🛠 Предварительная подготовка (Environment Setup)

Для выполнения всех заданий рекомендуется использовать локальную среду на базе Docker.

1.  **Установите Docker Desktop и Git.**
2.  **Склонируйте репозиторий с шаблонами:**
    ```bash
    git clone https://github.com/BosenkoTM/Data-Engineering-Platforms.git
    cd Data-Engineering-Platforms
    ```
3.  **Подготовьте структуру папок:**
    Создайте корневую папку `superstore_analytics`, внутри которой будут лежать проекты: `dwh_db` (для sql), `dbt_project`, `airflow_dags`.

---

## 📝 Задание 1. Базы данных и SQL-моделирование

**Цель:** Подготовить фундамент хранилища данных.

**Алгоритм выполнения:**
1.  **Запуск PostgreSQL:** Создайте файл `docker-compose.yml` для запуска Postgres (версия 13+).
    *   Пробросьте порт `5432`.
    *   Создайте базу данных `superstore`.
2.  **Моделирование:**
    *   Используя инструмент моделирования (например, SqlDBM или Draw.io), спроектируйте схему «Звезда» на основе Excel-файла Superstore.
    *   Выделите измерения: `customer_dim`, `product_dim`, `geo_dim`, `calendar_dim`.
    *   Выделите факты: `sales_fact`.
3.  **Реализация:**
    *   Напишите DDL скрипты (`CREATE TABLE`) для создания схем `stg` (для сырых данных) и `dw` (для измерений и фактов).
    *   Загрузите данные из CSV/Excel в `stg` таблицы (можно использовать DBeaver или `COPY` команду).
    *   Напишите SQL-скрипты для наполнения таблиц измерений и фактов из `stg` таблиц.

**Формат сдачи (в README):**
*   ER-диаграмма модели.
*   Пример DDL скрипта для таблицы фактов.
*   Скриншот `SELECT COUNT(*)` из созданных таблиц.

---

## 🔨 Задание 2. Построение DWH с dbt

**Цель:** Реализовать трансформацию данных по методологии ELT.

**Техническое задание:**
Развить архитектуру DWH до уровней: **Staging -> Intermediate -> Marts**.

1.  **Инициализация:**
    *   Установите `dbt-postgres`.
    *   Инициализируйте проект `dbt init superstore_dwh`.
    *   Настройте `profiles.yml` на ваш Docker-Postgres.
2.  **Разработка моделей:**
    *   **Staging:** Создайте `stg_orders.sql` (view, очистка сырых данных).
    *   **Intermediate:** Создайте `int_sales_orders.sql` (денормализация, join фактов и измерений).
    *   **Marts (Витрина):** Реализуйте модель `mart_valuable_dormant_customers.sql`.
        *   *Логика:* Клиенты из топ-25% по выручке, не покупавшие > 6 месяцев.
        *   *Подсказка:* Используйте оконные функции `PERCENT_RANK()` и фильтр по дате.
3.  **Тестирование:**
    *   В файле `schema.yml` добавьте тесты `unique` и `not_null` для первичных ключей.
    *   Добавьте тест `relationships` (referential integrity) между фактами и измерениями.

**Формат сдачи (в README):**
*   Скриншот Lineage Graph (из `dbt docs serve`).
*   Код SQL модели `mart_valuable_dormant_customers`.
*   Скриншот успешного выполнения команды `dbt test`.

---

## 📊 Задание 3. Business Intelligence (Yandex DataLens)

**Цель:** Визуализация инсайтов для бизнеса.

**Техническое задание:**
Создать дашборд "Мониторинг эффективности продаж Superstore".

1.  **Подключение:**
    *   Настройте подключение DataLens к вашей PostgreSQL (если база локальная, используйте `ngrok` или выгрузите витрины в CSV и загрузите их как файлы).
2.  **Создание чартов:**
    *   **KPI:** Общая выручка, Прибыль, Количество заказов (Индикаторы).
    *   **Динамика:** Продажи по месяцам (Линейная диаграмма).
    *   **Анализ клиентов:** LTV клиентов по сегментам (Столбчатая диаграмма).
    *   **Проблема:** Таблица со списком "Спящих ценных клиентов" (на основе витрины из Задания 2).
3.  **Сборка дашборда:**
    *   Добавьте селекторы (фильтры) по Дате и Региону.
    *   Разместите чарты на сетке.

**Формат сдачи (в README):**
*   Скриншот итогового дашборда.
*   Краткий вывод: "В каком регионе самая низкая маржинальность?" (ответ найти с помощью дашборда).

---

## 🚀 Задание 4. Оркестрация с Apache Airflow

**Цель:** Автоматизация обновления хранилища.

**Техническое задание:**
Использовать локальный Docker-образ Airflow для запуска dbt.

1.  **Развертывание:**
    *   Используйте `docker-compose.yaml` из материалов курса (папка `local-development`).
    *   Смонтируйте папку с dbt-проектом (из Задания 2) в контейнер Airflow.
2.  **Разработка DAG:**
    *   Создайте файл `dags/superstore_pipeline.py`.
    *   Определите `BashOperator` для задач:
        1.  `dbt_run`: выполняет команду `dbt run --profiles-dir .`
        2.  `dbt_test`: выполняет команду `dbt test --profiles-dir .`
    *   Настройте зависимость: `dbt_run >> dbt_test`.
3.  **Запуск:**
    *   Запустите DAG в интерфейсе Airflow (localhost:8080).

**Формат сдачи (в README):**
*   Код файла `superstore_pipeline.py`.
*   Скриншот "Grid View" или "Graph View" в Airflow, где все задачи отмечены зеленым цветом (success).

---

# Критерии оценки практических заданий

### Задание 1. Развертывание базы данных и SQL-моделирование (100%)

*   **30% — Проектирование модели данных (Схема «Звезда»):**
    *   Корректно определены и разделены таблицы фактов (`sales_fact`) и таблицы измерений (`customer`, `product`, `geo`, `calendar`).
    *   Определены первичные и внешние ключи.
*   **30% — Техническая реализация (DDL и Docker):**
    *   Успешный запуск PostgreSQL в контейнере Docker.
    *   Скрипты создания таблиц (DDL) написаны без синтаксических ошибок и соответствуют спроектированной модели.
*   **20% — ETL/ELT процесс (Загрузка данных):**
    *   Сырые данные успешно загружены в `staging` слой.
    *   Написаны и выполнены скрипты наполнения целевых таблиц (`INSERT INTO ... SELECT`).
*   **20% — Отчетность:**
    *   Представлена ER-диаграмма.
    *   Приложены скриншоты подтверждения наличия данных (результаты `SELECT COUNT(*)`).

---

### Задание 2. Построение DWH с использованием dbt (100%)

*   **20% — Архитектура проекта:**
    *   Соблюдена структура слоев: `models/staging` (view), `models/intermediate`, `models/marts` (table).
    *   Проект корректно сконфигурирован (`dbt_project.yml`, `profiles.yml`).
*   **40% — Сложная бизнес-логика (Mart Model):**
    *   Модель `mart_valuable_dormant_customers` реализована верно.
    *   Правильно применены оконные функции (для ранжирования по выручке) и фильтрация по датам (для определения "спящих" клиентов).
*   **30% — Качество данных и документация:**
    *   Определены тесты в `schema.yml` (`unique`, `not_null`, `relationships`).
    *   Все тесты проходят успешно (скриншот `dbt test`).
    *   Сгенерирован и представлен граф зависимостей (Lineage).
*   **10% — Репозиторий:**
    *   Код чистый, загружен в Git, структура папок соответствует стандартам dbt.

---

### Задание 3. Создание BI-дашборда в Yandex DataLens (100%)

*   **40% — Визуализация и информативность:**
    *   Создано минимум 3 типа разноплановых чартов (KPI-индикаторы, линейные графики динамики, столбчатые диаграммы или таблицы).
    *   Визуализации отвечают на поставленные бизнес-вопросы (выручка, LTV, проблемные зоны).
*   **30% — UX и Интерактивность:**
    *   Настроены глобальные селекторы (фильтры) по Дате и Региону/Категории.
    *   Дашборд скомпонован логично, элементы не наезжают друг на друга, нет визуального шума.
*   **20% — Аналитические выводы:**
    *   В отчете дан верный ответ на контрольный вопрос (например, про маржинальность региона), полученный с помощью созданного инструмента.
*   **10% — Техническая настройка:**
    *   Корректно настроено подключение к источнику данных и типизация полей в датасете.

---

### Задание 4. Оркестрация процессов с Apache Airflow (100%)

*   **40% — Код DAG (Python):**
    *   Синтаксически верный Python-код.
    *   Использование корректных операторов (например, `BashOperator` для вызова dbt).
    *   Правильно настроены зависимости задач (битовые операторы `>>`): сначала трансформация (`run`), затем тесты (`test`).
*   **30% — Интеграция окружения:**
    *   Docker-контейнер Airflow корректно настроен на работу с файлами проекта (volume mounting) и базой данных.
    *   Профиль подключения dbt внутри Airflow настроен верно.
*   **30% — Выполнение пайплайна:**
    *   DAG успешно прошел выполнение (Status: Success).
    *   Предоставлен скриншот Grid/Graph View с зелеными индикаторами всех задач.
```





